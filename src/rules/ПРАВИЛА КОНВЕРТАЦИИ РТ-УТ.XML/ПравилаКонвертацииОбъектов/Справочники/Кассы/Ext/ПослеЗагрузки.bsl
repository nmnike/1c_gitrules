Если Параметры.УзелДляОбменаОбъект.мЕстьФильтрПоКассам И Не Параметры.УзелДляОбменаОбъект.ЭлементВходитВТаблицу(Объект.Ссылка, "Кассы", "Касса") Тогда
	
	Отказ = Истина;	
	
КонецЕсли;

Если НЕ Объект.ЭтоГруппа Тогда
	
	Если НЕ (Отказ И ЗначениеЗаполнено(Объект.ВалютаДенежныхСредств)) Тогда
		
		Объект.ВалютаДенежныхСредств = Константы.ВалютаРегламентированногоУчета.Получить();
		
	КонецЕсли;
	
КонецЕсли;
